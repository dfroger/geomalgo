package:
  name: geomalgo
  version: 0.0.3

source:
    fn: geomalgo-0.0.3.tar.gz
    url: https://github.com/dfroger/geomalgo/archive/0.0.3.tar.gz

build:
  number: 0
  binary_relocation: true

requirements:
  build:
    - python
    - cython
  run:
    - python
    - cython

test:
    imports:
        - geomalgo

about:
  home: https://github.com/dfroger/geomalgo
  license: BSD
  summary: Cython implementation of http://geomalgorithms.com
